import 'package:flutter/material.dart';

class HijriCalendar extends StatelessWidget {
  const HijriCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hijri Calendar"),
    );
  }
}